---
description: File the work from this session into the Obsidian second brain
---

Save the substantive work from this conversation into the vault at `__VAULT_PATH__`.

Use the `second-brain` skill. In short:

1. Read `__VAULT_PATH__/CLAUDE.md` for the convention.
2. Pick the branch that fits. Create the folder + its MOC if it doesn't exist yet.
3. Write the note from `3-Other 📦/Templates/Note Template.md` — opening callout with the key fact,
   emoji sections, absolute dates, wikilinks.
4. Copy any referenced file (PDF, image, export) into the folder and embed it with `![[file]]`.
5. Add the note to that folder's `## 📌 Hub Documents` table.
6. Report the vault-relative path.

$ARGUMENTS — if given, treat as the topic or title to file under. If empty, decide from the
conversation and say what you chose.

Never write secrets into the vault.
